{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "rimraf dist && observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable",
    "test": "NODE_OPTIONS='--experimental-modules --experimental-vm-modules --experimental-specifier-resolution=node' jest"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.4",
    "d3-dsv": "^3.0.1",
    "d3-sankey": "0.12.3",
    "d3-time-format": "^4.1.0",
    "jest": "^30.3.0",
    "lodash": "4.17.21",
    "temporal-polyfill": "0.3.2"
  },
  "devDependencies": {
    "dotenv": "17.3.1",
    "octokit": "5.0.5",
    "rimraf": "^6.1.3",
    "yaml": "2.8.2"
  },
  "engines": {
    "node": ">=18"
  }
}
